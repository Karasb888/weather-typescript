import * as React from 'react';
import CityForm from './CityForm';

const Header: React.FC = () => {
    return (
        <header>
            <CityForm />
        </header>
    );
};

export default Header;
